const student_info = () => {
    const name = "Nazrul";
    const address = "Demre";

    return name + "<br>" + " lived in " + address;
    //`Name: ${name} Address: ${address}`;
}

let country = "Bangladesh";
let district = "Dhaka"
 export default student_info;
 export {country,district}